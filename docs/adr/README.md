# Architecture decision records

Architecture decision records (ADRs) capture decisions that are expensive or
risky to reverse. Each ADR states its context, decision, alternatives,
consequences, and future review triggers.

ADRs use zero-padded sequential names:

```text
0001-modular-monolith.md
```

Accepted decisions remain historical records. A later ADR supersedes an earlier
one rather than rewriting it.
